function preload() {
}

function setup() {
    canvas = createCanvas(300, 300);
    canvas.center();
    canvas.position(660, 200);
}

function draw() {
}

function take_snapshot() {
    save('myFilterImage.png');
}